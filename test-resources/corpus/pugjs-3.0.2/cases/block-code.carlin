- (let [list ["uno" "dos" "tres"
              "cuatro" "cinco" "seis"]])
  //- Without a block, the element is accepted and no code is generated
  -
  each item in list
    - (let [string (str (.toUpperCase (subs item 0 1))
                        (subs item 1))])
      li= string
